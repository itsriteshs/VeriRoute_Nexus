# Owner: Person 1 — Backend + Algorithms Lead
# Purpose: hardware API routes.

from fastapi import APIRouter

router = APIRouter(prefix="/hardware", tags=["hardware"])

# TODO: Person 1 will align this router with API_CONTRACT.md.
