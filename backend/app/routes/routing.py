# Owner: Person 1 — Backend + Algorithms Lead
# Purpose: routing API routes.

from fastapi import APIRouter

router = APIRouter(prefix="/routing", tags=["routing"])

# TODO: Person 1 will align this router with API_CONTRACT.md.
