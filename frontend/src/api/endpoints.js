// Owner: Person 2 — Frontend + Digital Twin + UX Lead
// Purpose: Backend endpoint constants mirrored from API_CONTRACT.md.

export const endpoints = {
  health: '/health', seedDemo: '/demo/seed', resetDemo: '/demo/reset', hubs: '/hubs', edges: '/edges', parcels: '/parcels', scan: '/scan', hardwareScan: '/hardware/scan', metrics: '/metrics',
};
