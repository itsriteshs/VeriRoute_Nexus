// Owner: Person 2 — Frontend + Digital Twin + UX Lead
// Purpose: reusable metric card

export default function MetricCard() {
  return <section className="panel"><h2>MetricCard</h2><p>TODO: Build reusable metric card.</p></section>;
}
